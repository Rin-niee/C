#include "lab9c.h"

int main() 
{ 	char *prim;
	prim=((char*)malloc(strlen(prim) +1));
	double result;
	 printf("Enter an example: \n"); 
	 scanf(" %s", &prim);
	 result= schet(prim);  
	 printf("Result: %lf\n", result); 
	 return 0; 
}
