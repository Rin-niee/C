#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) 
{
	int sum1, sum2, i, n, j, k;
	double mas[n][n];
	k =0;
	for(i=0; i<n; i++)
	sum1=0;
	sum2=0;
	sum1+=mas[i][i];
	sum1+=mas[i][(n-1)-i];
	printf("ñóììà ãëàâíîé äèàãîíàëè ðàâíà:", %d, sum1);
	printf("ñóììà ïîáî÷íîé äèàãîíàëè ðàâíà:", %d, sum2);
	return 0;
}
