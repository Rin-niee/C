#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

typedef struct
{
	char lastname[60];
	char name[60];
	int birthyear;
	char gender[60];
	float height; 
} human;

int main() 
{	human humens[16];
	human hus;
	int i, j, h, h1, k, n, h0; 
	char c[100];
	char c1[100];
	char c2[100];
	char a[100];
	char s[1000];
	h=0;
	FILE* f;
	f = fopen("text.txt", "r");
	if (f==NULL)
	{
		printf("Error");
	}
	else
	{	
		for (i=0; i<16; i++)
		{
			fscanf(f, "%s %s %d %s %f", humens[i].lastname, humens[i].name, &humens[i].birthyear, &humens[i].gender, &humens[i].height);
			//printf("%s %s %d %s %.2f\n", humens[i].lastname, humens[i].name, humens[i].birthyear, humens[i].gender, humens[i].height);		
		}
		printf("Enter parameter(lastname, name, birthyear, gender, height, multi):\n");
		gets(c);
		for(i=0; i<16; i++)
		{
			for(j=0; j<16; j++)
			{
				if (strcmp(c, "name")==0)
			 	{
			 		h0= strcmp(humens[i].name, humens[j].name);
				}
				else if (strcmp(c, "lastname")==0)
			 	{
			 		h0= strcmp(humens[i].lastname, humens[j].lastname);
				}
				else if (strcmp(c, "year")==0)
			 	{
			 		h0 = humens[i].birthyear- humens[j].birthyear;
				}
				else if (strcmp(c, "gender")==0)
			 	{
			 		h0= strcmp(humens[i].gender, humens[j].gender);
				}
				else if (strcmp(c, "height")==0)
				{
			 		h0 = humens[i].height - humens[j].height;
				} 
				if (h0>0)
				{
					hus = humens[i];
					humens[i]=humens[j];
					humens[j]=hus;
				}
				if (strcmp(c, "multi")==0)
				{
					printf("Enter two parameters:\n");
					scanf("%s %s", &c1, &c2);
					if (strcmp(c1, c2)==0)
					{
						printf("ERROR");
					}
					else
					{
					if (strcmp(c1, "name")==0)
				 	{
				 		h= strcmp(humens[i].name, humens[j].name);
					}
					else if (strcmp(c1, "lastname")==0)
				 	{
				 		h= strcmp(humens[i].lastname, humens[j].lastname);
					}
					else if (strcmp(c1, "year")==0)
				 	{
				 		h = humens[i].birthyear- humens[j].birthyear;
					}
					else if (strcmp(c1, "gender")==0)
				 	{
				 		h= strcmp(humens[i].gender, humens[j].gender);
					}
					else if (strcmp(c1, "height")==0)
					{
				 		h = humens[i].height - humens[j].height;
					} 
					if (strcmp(c2, "name")==0)
					{
					 	h1= strcmp(humens[i].name, humens[j].name);
					}
					else if (strcmp(c2, "lastname")==0)
					{
					 	h1= strcmp(humens[i].lastname, humens[j].lastname);
					}
					else if (strcmp(c2, "year")==0)
					{
					 	h1 = humens[i].birthyear- humens[j].birthyear;
					}
					else if (strcmp(c2, "gender")==0)
					{
					 	h1= strcmp(humens[i].gender, humens[j].gender);
					}
					if (strcmp(c2, "height")==0)
					{
					 	h1 = humens[i].height - humens[j].height;
					}
					if (h>0)
					{
						hus = humens[i];
						humens[i]=humens[j];
						humens[j]=hus;
					}
					if (h1>0)
					{
						hus = humens[i];
						humens[i]=humens[j];
						humens[j]=hus;
					}
				}	
			}
		}
	}
}
	for (k=0; k<16; k++)
	{
		printf("%s %s %d %s %.2f\n", humens[k].lastname, humens[k].name, humens[k].birthyear, humens[k].gender, humens[k].height);		
	}
	return 0;	
}
