#include "8lab.h"

int main(int argc, char *argv[]) 
{
	int i, n;
	humen hu[1000];
	char a[16];
	FILE* f;
	f = fopen("text1.txt", "r");
	if (f == NULL)
	{
		printf("ERROR!");
	}
	else
	{
		fscanf(f, "%d", &n);
		for (i = 0; i<n; i++)
		{
			fscanf(f, "%s %s %d %c %f", hu[i].name, hu[i].lastname, &hu[i].birthyear, &hu[i].gender, &hu[i].height);
		}
		fclose(f);
		printf("sort for(name, lastname, birthyear, gender, height):\n");
		scanf("%s ", &a);
		for (i =0; i<1000; i++)
			{
			printf("Sort %s:\n", a[i]);
			sortarray(hu, n, a[16]);
			}
		}
	return 0;	
}
