#include "8lab.h"

int main(int argc, char *argv[]) 
{
	int i, n;
	humens hu;
	char c[4][16]= {"name", "lastname", "birthyear", "gender", "height"};
	char a[16];
	FILE* f;
	f = fopen("text1.txt", "r");
	if (f == NULL)
	{
		printf("ERROR!";)
	}
	else
	{
		fscanf(f, "%d", &n);
		hu= (humen*)malloc(n*sizeof(humen));
		for (i = 0; i<n; i++)
		{
			fscanf("%s %s %d %c %f", humen[i]->name, humen[i]->lastname, &humen[i]->birthyear, &humen[i]->gender, &humen[i]->height);
		}
		fclose(file);
		printf("sort for(name, lastname, birthyear, gender, height, multi):\n");
		scanf("%s ", &a);
		if (strcmp(a, "multy")==0)
		{
			for (i =0; i<4; i++)
			{
				printf("Sort %s:\n", a[i]);
				sortarray(hu, n, a[16]);
			}
		}
	}
return 0;
}
