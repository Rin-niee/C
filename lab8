  #include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct
{
	char lastname[60];
	char name[60];
	int birthyear;
	char gender[60];
	float height; 
} human;

int main() 
{	human humens;
	int i, j, h; 
	char c[100]; //çàïèñü ñòðîêè, â êîòîðîé ïîñëå áóäåò ðàñïîëàãàòüñÿ îïðåäåëèòåëü;
	char s[10000];
	FILE* f;
	f = fopen("text.txt", "r");
	if (f==NULL)
	{
		printf("Error");
	}
	else
	{
		while(fgets(s, 10000, file)!=NULL)
		{ 
			for (i=0; i<10000; i++)
				{
					scanf("%s %s %d %s %f", humens[i].lastname, humens[i].name, humens[i].birthyear, humens[i].gender, humens[i].height);
				}
			}
		}
		printf("Enter parameter:\n");
		gets(c);
		for(i=0; i<10000; i++)
		{
			for(i=0; i<10000; i++)
				{
					
				}
		}
	}
	return 0;	
}
