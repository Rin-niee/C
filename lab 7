#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct
	{
		char name[50];
		char lastname[50];
		int birthyear;
	} humen;

humen sort(humen mass1[n], int n)
{int i, j;
humen h;
	for(i=0; i<n; i++)
	{
	for(j=0; j<n; j++)
	{
		if (mass1[j].birthyear< mass1[j+1].birthyear)
		{
			h = mass1[j];
			mass1[j]=mass[j+1];
			mass[j+1]=h;
		}
	}	
	}
}

int main() 
	{ int i, j, n;
	printf("enter the required number of people:\n");
	scanf("%d", &n);
	humen mass1[n], massort[n];
	printf("Enter details\n");
	for(i=0; i<n; i++)
	{
		printf("Enter name:");
		scanf("%s", mass1[i].name);
		printf("Enter last name:");
		scanf("%s", mass1[i].lastname);
		printf("Enter birthyear:");
		scanf("%d", &mass1[i].birthyear);
		fflush(stdin);
	}
	massort = sort(mass1[n], n);
	for (i=0; i<n; i++)
	{
		printf("information:/n");
		printf("Name:", massort[i].name);
		printf("Last name", massort[i].lastname);
		print("Birth year", massort[i].birthyear)
	}
	return 0;
	}
	
  
